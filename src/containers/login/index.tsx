import React, { ReactElement } from 'react'

interface Props {
  email: string;
  password: string;
}

const Login: React.FC<Props> = ({ email, password }) => {
  return (
    <div>

    </div >
  )
}

export default Login
